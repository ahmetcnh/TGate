param N = 4;

gate INV_PARAM(in a[N]; out y[N]) {
    generate i from 0 to N-1 {
        pmos drain=y[i], gate=a[i], source=VDD;
        nmos drain=y[i], gate=a[i], source=GND;
    }
}

simulate INV_PARAM(a=[0, 1, 0, 1]);
