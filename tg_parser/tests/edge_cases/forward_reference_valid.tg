circuit UseINV(in x; out z) {
    INV g1(x) -> z;
}

gate INV(in a; out y) {
    pmos drain=y, gate=a, source=VDD;
    nmos drain=y, gate=a, source=GND;
}

simulate UseINV(x=0);
simulate UseINV(x=1);
